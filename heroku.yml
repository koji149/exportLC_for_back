build:
  docker:
    web: Dockerfile
    # nginx: nginx/Dockerfile
run:
  web: ./config/start-nginx-solo.sh
  # web: /bin/sh/bin/start-nginx bundle exec unicorn -p $PORT -c ./config/unicorn_production.rb
